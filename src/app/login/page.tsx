"use client";

import { LoginView } from "@/components/auth/LoginView";

/**
 * Login Page Entry Point
 * Architecture: Entry shell rendering a modular Pure View.
 */
export default function LoginPage() {
  return <LoginView />;
}
